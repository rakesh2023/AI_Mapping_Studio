/* =========================================================================
   introduction.js - Introduction / getting-started page.
   Static overview + workflow guide; just mounts the shared app shell.
   ========================================================================= */
document.addEventListener("DOMContentLoaded", async () => {
  await initShell("introduction.html");
});
